To do:


- Style
- Mobile format
- When the same two currencies are selected nothing updates


Possible to do:

- On converter, make inputs only editable once two countries have been selected
- Make it only do the API call once and store the rates, and then calculate based on that